# Dependencies (none required, but just in case)
node_modules/

# Local environment
.env
.env.local

# Runtime data (leads, outbox). Do NOT commit real lead data (GDPR).
data/*.json
!data/.gitkeep

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
